services:
  caddy:
    image: caddy-extended:latest
    build:
      context: .
      dockerfile: dockerfile
    container_name: caddy
    env_file:
      - path: .env
        required: false
    networks:
      - caddy_network
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./config:/etc/caddy
      - ./static:/srv
      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_data:
  caddy_config:

networks:
  caddy_network:
    name: caddy_network
    driver: bridge
